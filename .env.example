# Copy to .env and fill in local dev config/secrets. Never commit .env.
#
# No variables are required yet. Add them here with placeholder values as
# the app starts reading them, one KEY=value per line, e.g.:
#
# ALTINN_API_KEY=replace-me
